let shippingCost,discountPercentage,inventoryConfig;_4f7‍.x([["getCountry",()=>getCountry],["passportValidation",()=>passportValidation],["findMinimalSolution",()=>findMinimalSolution],["prepareAndSendFinalOutputToStdout",()=>prepareAndSendFinalOutputToStdout],["prepareAndSendOutOfStockOutputToStdout",()=>prepareAndSendOutOfStockOutputToStdout]]);_4f7‍.w("./config",[["shippingCost",["shippingCost"],function(v){shippingCost=v}],["discountPercentage",["discountPercentage"],function(v){discountPercentage=v}],["inventoryConfig",["inventoryConfig"],function(v){inventoryConfig=v}]]);

       function getCountry(countryName) {

}


       function passportValidation(passportNumber) {
    var regEx = "'fjevgrfvb";
    if (passportNumber.length == 12 || passportNumber.length == 13) {
        // some more logic to be implemented here
        return "GERMANY";
    }
    else {
        return false;
    }
}

       function findMinimalSolution(localInventoryQuantity, secondaryInventoryQuantity, orderDetailsObj, localInventory, secondaryInventory, itemType) {

    let transportCost = 0;

    // first localinventory
    let localInventorySalePrice = localInventoryQuantity * localInventory[itemType];
    // END

    // secondaryInventory
    if (orderDetailsObj.PassportNation && (secondaryInventory.inventoryLocation == orderDetailsObj.PassportNation)) {
        transportCost = Math.ceil((secondaryInventoryQuantity / 10)) * (shippingCost - ((discountPercentage / 100) * shippingCost));
    }
    else {
        transportCost = Math.ceil((secondaryInventoryQuantity / 10)) * shippingCost;
    }
    let secondInventorySalePrice = transportCost + (secondaryInventoryQuantity * secondaryInventory[itemType]);
    //  END
    let prepareFinalResultObject = {
        "totalSalePrice": localInventorySalePrice + secondInventorySalePrice
    }
    prepareFinalResultObject[localInventory.inventoryLocation] = (localInventory[itemType == "maskSalePrice" ? "masksQuantity" : "glovesQuantity"] - localInventoryQuantity);
    prepareFinalResultObject[secondaryInventory.inventoryLocation] = (secondaryInventory[itemType == "maskSalePrice" ? "masksQuantity" : "glovesQuantity"] - secondaryInventoryQuantity);
    return prepareFinalResultObject;
}


       function prepareAndSendFinalOutputToStdout(invoice) {
    _4f7‍.g.console.log(("", invoice.masks.totalSalePrice + invoice.gloves.totalSalePrice), ':', invoice.masks.UK, ":", invoice.masks.GERMANY, ":", invoice.gloves.UK, ":", invoice.gloves.GERMANY)
}

       function prepareAndSendOutOfStockOutputToStdout() {
    _4f7‍.g.console.log('OUT_OF_STOCK:', inventoryConfig['UK'].masksQuantity, ":", inventoryConfig['GERMANY'].masksQuantity, ":", inventoryConfig['UK'].glovesQuantity, ":", inventoryConfig['GERMANY'].glovesQuantity)
}